version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - rowserrcheck
    - sqlclosecheck
    - copyloopvar
    - forbidigo
  settings:
    errcheck:
      check-type-assertions: false
      check-blank: true
    govet:
      enable:
        - copylocks
    forbidigo:
      forbid:
        - pattern: 'regexp\.MustCompile\(\s*[^"`]'
          msg: "use a literal regexp.MustCompile argument (precompile at package scope)"
        - pattern: 'regexp\.Compile\(\s*[^"`]'
          msg: "use a literal regexp.Compile argument (precompile/cache patterns)"
        - pattern: 'context\.Background\(\)'
          msg: "use a propagated context; context.Background is only allowed in main and tests"
        - pattern: '\blog\.(Fatal|Fatalf)\('
          msg: "avoid process-fatal logging outside main/tests"
        - pattern: '\bos\.Exit\('
          msg: "avoid process exits outside main/tests"
        # Timestamp provenance is NOT enforced here — see telegen/AGENTS.md
        # "Timestamp Provenance".
        #
        # forbidigo matches the call expression text WITHOUT its argument list,
        # so a bare `time.Now()` presents as `time.Now` while a chained call
        # presents as `time.Now().Add`. A `time.Now()` pattern therefore matches
        # only chained deadline/TTL/backoff arithmetic (all legitimate) and never
        # the bare per-metric clock read it was meant to catch. Regex also cannot
        # see whether a call sits inside an emit loop, which is the real
        # invariant. Enforcement lives where it can be exact:
        #   - types.NewMetricAt / NewCounterMetricAt take a mandatory timestamp,
        #     so the compiler rejects any per-metric clock read.
        #   - collector tests assert the hoisted instant reaches every metric.
    staticcheck:
      checks:
        - all
        - -QF*    # Disable quickfix suggestions
        - -ST*    # Disable style suggestions
        - -SA9003 # Allow empty branches
        - -SA1019 # Allow deprecated functions
        - -S1008  # Allow if/return instead of direct return
  exclusions:
    rules:
      # Ignore errcheck and unused in test files
      - path: _test\.go
        linters:
          - errcheck
          - unused
          - forbidigo
          - bodyclose
      # main entrypoints are the only allowed production sites for process exit
      # and root context construction.
      - path: ^cmd/.*/main\.go
        linters:
          - forbidigo
      # Ignore errcheck for defer statements
      - source: "defer .*\\.Close\\(\\)"
        linters:
          - errcheck
      - source: "defer .*\\.Stop\\(\\)"
        linters:
          - errcheck
      # Ignore in dev directory (prototype code)
      - path: ^dev/
        linters:
          - errcheck
          - unused
          - staticcheck
          - ineffassign
      # Ignore errcheck in queue (disk operations may fail on cleanup)
      - path: internal/queue/
        linters:
          - errcheck
      # Ignore unused in internal packages (may be used in build constrained files)
      - path: ^internal/
        linters:
          - unused
      # Ignore ineffassign in prometheus adapter (parser pattern)
      - path: prometheus_adapter\.go
        linters:
          - ineffassign
